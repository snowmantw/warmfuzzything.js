/**
 * 1. Consider how to begin from wrapping value and monadic value
 * 2. The Monadic computation is the only obvious missing feature
 * 3. The promise mixed style is a good way to do that
 */
(function(exports) {
'use strict;
  var ColorCharacters = function() {};
  ColorCharacters.prototype.colorString =
  function(str) {
    
  };
  exports.ColorCharacters = function() {};
})(window);
